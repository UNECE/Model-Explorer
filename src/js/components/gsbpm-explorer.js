import React from 'react';

function GSBPMExplorer({ loaded, phases }) {
  return (
    <ul>
      { [1, 2, 3].map(() =>
        <li>phases</li>
      )}
    </ul>
  );
}

export default GSBPMExplorer;
